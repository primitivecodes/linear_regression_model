name: flutter_app
description: Crop Yield Prediction App
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: "^3.12.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.2

flutter:
  uses-material-design: true